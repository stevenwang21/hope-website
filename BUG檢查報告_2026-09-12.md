# 網站 Bug 檢查報告 · 2026-09-12

檢查對象：`index.html`（11,033 行 / 389KB 單檔），另快速核對 `courses.html`、`tracking.html`、`dates.json`、`scripts/fetch-blogger.js`。

> **檢查方式說明**：這次是排程自動執行，瀏覽器實測需要授權開啟 `new.ourhope.com.tw`，
> 無人在場授權所以被擋下，因此改用「完整靜態程式碼分析」+ 一個可在本機開啟的對照測試頁。
> 下面標「嚴重／中」的三項都有明確的 CSS 特異性依據，不是猜測；標「小」的偏建議。

---

## 一、發現的問題

### 🔴 #1【嚴重】手機版沒有入口可以進到一半的分頁
- 位置：`index.html` 7557–7558 行
```css
@media (max-width: 720px) {
  .menu-toggle { display: none !important; }
  .nav-links  { display: none !important; }
}
```
- 漢堡選單和整個導覽選單在 720px 以下被 `!important` 關掉，只剩底部導覽列。
- 底部導覽列只有 5 項：**首頁 / 聯絡簿 / 課程 / 線上 / 預約**。
- 而且 `.nav-links.active`（6653 行）沒有 `!important`，就算 JS 把 `active` 加上去也打不開。
- **結果：手機上「關於我們、師資、行事曆、榜單、好評」完全沒有任何入口。**
  （頁尾的連結也不能救，見 #2。）

### 🟠 #2【中】頁尾在每一頁都看不到（桌機也一樣）
- 位置：6812 行 `body.is-paged footer { display: none; }`
- `is-paged` 由 JS 無條件加在 `<body>`（10622 行），所以頁尾永遠是隱藏的。
- 消失的內容：電話、地址、營業時間、LINE / Facebook、關於／聯絡簿快速連結、© 版權。
- 註解寫「一頁一主題模式下會顯得多餘」，**所以有可能是刻意的** —— 這點想請你確認。

### 🟠 #3【中】首屏（Hero）垂直對齊會跑掉
- `.hero { display: flex !important; align-items: flex-end !important; }`（354–355 行）
- 但 `body.is-paged[data-current="home"] section[data-page="home"] { display: block !important; }`（6800–6811 行）特異性更高 → 實際生效的是 `display: block`。
- 結果 `align-items: flex-end` 失效，首屏內容會貼上面而不是貼底部。

### 🟡 #4【小】未開放的聯絡簿卡片看起來能點、點了沒反應
- `href="javascript:void(0)"`：高一英文、高二數學、高三物理／化學／數學／英文（8435、8470、8495、8505、8515、8525 行）。
- 外觀跟可用的卡片一模一樣，家長會以為網站壞了。

### 🟡 #5【小】HTML 無效屬性
- 10170 行 Google 地圖 `<iframe ... loading="lazy" decoding="async">` —— `decoding` 不是 iframe 的屬性（只有 `<img>` 有）。不影響顯示，但會被驗證器報錯。

### 🟡 #6【小】首頁「最新消息」已經過期
- 彈窗（7885 行）與行事曆卡片（9531 行）都還是「**7 月 3 日 升八年級理化開課 · 現在開始登記，預約劃位中**」。
- 今天是 9/12，已經過了 2 個多月。行事曆第二張卡也還是「TBA 敬請期待」。
- 這是內容問題，**我沒有動它**，需要你給新文案。

### 🟡 #7【小】正式版裡留著「測試模式」的金流程式碼
- 8830–8878 行 + 9202–9213 行：線上課程購物車有假匯款帳號 `0000-000-000000`、
  「測試模式：點下方按鈕會立刻開通章節」、佔位 LINE 群 `https://line.me/R/ti/p/@example`（9185 行）。
- 目前 `#onlinePanelG12` 是 `hidden style="display:none"`，六個年級卡片都直接連到 `courses.html`，
  **所以使用者點不到**。但這段程式碼還在正式版裡，哪天把面板打開就會外露。建議刪掉或搬到獨立檔案。

### 🟡 #8【小】死碼（不影響功能）
- 6831–6851 行整組 `body.mobile-pager` 規則：JS 實際用的是 `is-paged`，`mobile-pager` 從沒被加上去。
- 6657 行 `.logo-sub` 選擇器：HTML 裡沒有這個 class（實際是 `logo-slogan` / `logo-subjects`）。
- 10976 行 `(function initBloggerHomework() {})();` 空函式。

### 🟡 #9【小】錯字／命名
- HTML 註解裡「手機**板**」應為「手機**版**」（8573、8799 行附近，不會顯示給使用者）。
- 資料夾裡 `繳學金申請pdf.pdf` → 應為「**獎**學金」（此檔沒被任何頁面引用）。
- `114國中部第三次段考更該三 拷貝.jpg` → 「更**該**三」疑為「更**改**三」（引用路徑正確，只是檔名怪）。
- `dates.json` 的 `11-english` 標題寫「2025年8月8日」但日期是 `2026-08-08` —— 這是 Blogger 原文標題打錯，不是網站的 bug。

---

## 二、檢查通過、沒有問題的部分

| 檢查項 | 結果 |
|---|---|
| 圖片破圖 | ✅ 全部 40+ 張引用（含 `.JPEG` 教室照、`scholarship/page-1~9.jpg`、`115升九-各科上課時間(含小團班).jpg`）檔案都在，無破圖 |
| 導覽列連結 | ✅ 10 個 `#` 錨點（home/about/courses/dm/online/teachers/calendar/honor/testimonials/contact）對應的 `id` 全部存在 |
| 內頁連結 | ✅ `courses.html`、`tracking.html` 存在，兩頁的 `index.html` 回連也正常 |
| 外部連結 | ✅ 15 個 blogspot 聯絡簿、6 支 YouTube、LINE、FB、Google Maps 格式都正確，`target="_blank"` 都有配 `rel="noopener"` |
| 聯絡簿日期 | ✅ `dates.json` 有資料（最後更新 2026-09-10），前端有 localStorage 快取 + 抓失敗的備援文字，不會卡在「載入中...」 |
| 諮詢表單 | ✅ 有真的送出去（隱藏 iframe POST 到 Google 表單），不是只跳成功訊息 |
| HTML 標籤閉合 | ✅ `<div>` 487/487、`<section>` 17/17 對稱 |
| 榜單數字 | ✅ 「合計上榜 38 人次」= 2+10+5+6+3+6+6 正確；長條圖寬度與人次比例一致 |
| 大事紀年份 | ✅ 民國 76/79/86/94–102/102/104 ↔ 1987/1990/1997/2005–2013/2013/2015 全部換算正確 |
| 按鈕 / 彈窗 | ✅ 最新消息、老師試聽影片 ×4、DM 燈箱、榜單燈箱、獎學金放大、教室照片、FAQ、國中／高中切換、Hero 輪播 —— 開關與 Esc 關閉邏輯都完整 |

---

## 三、已修正的內容（都在測試檔，正式版未動）

修正做成「外掛補丁」形式，**一行都沒改 `index.html`**：

| 檔案 | 內容 |
|---|---|
| `bugfix-patch.css` | #1 手機版把漢堡選單放回來、`.nav-links.active` 補 `!important` 讓它打得開<br>#2 只在「預約諮詢」分頁把頁尾放回來（保守做法）<br>#3 用更高特異性把 Hero 的 `display:flex` + `align-items:flex-end` 拿回來<br>#4 停用中的聯絡簿卡片改成明顯的灰階／不可點外觀 |
| `bugfix-patch.js` | #4 把 `javascript:void(0)` 的卡片標成 `aria-disabled`、擋掉點擊<br>#5 移除 iframe 上無效的 `decoding` 屬性<br>#6 從底部導覽切分頁時順手收起手機選單 |
| `bugfix-test.html` | 對照測試頁：把 `index.html` 嵌進 iframe 再即時注入上面兩個檔，有「修正開／關」切換和桌機／平板 820/手機 390 三種寬度 |

### 怎麼看效果
1. 先跑 `啟動本機伺服器.bat`（一定要用 http，直接雙擊開檔會被瀏覽器 file:// 限制擋住注入）。
2. 瀏覽器開 `http://localhost:<埠號>/bugfix-test.html`。
3. 選「手機 390px」，按右上角「修正：已套用 / 關閉（原始版）」來回切，就能看出 #1 的差別。

### 確認 OK 後要怎麼正式套用
在 `index.html` 加兩行就好（不用改任何現有程式碼）：
```html
<!-- </head> 前 -->
<link rel="stylesheet" href="bugfix-patch.css" />
<!-- </body> 前 -->
<script src="bugfix-patch.js"></script>
```

---

## 四、修改到的檔案

**新增 4 個檔（都在 `ourhope網站` 資料夾）：**
- `bugfix-patch.css`
- `bugfix-patch.js`
- `bugfix-test.html`
- `BUG檢查報告_2026-09-12.md`（本檔）

**沒有動到的檔：** `index.html`、`courses.html`、`tracking.html`、`dates.json`、所有圖片。

> 另外提醒：資料夾裡有一個舊的 `index_bugfix_test.html`，是比現在的 `index.html` 更舊的整份副本
> （`robots` meta 還是舊的）。我沒有動它，但它已經過期了，建議刪掉以免以後拿錯檔。

---

## 五、需要你確認的地方

1. **頁尾要不要顯示？**（#2）程式碼註解說是刻意隱藏的。我目前只在「預約諮詢」頁放回來。
   想每頁都顯示的話，把 `bugfix-patch.css` 裡的選擇器改成 `body.is-paged footer`；
   想維持全部隱藏，就把那一段刪掉。
2. **手機導覽想用哪種解法？**（#1）我選的是「把漢堡選單放回來」（改動最小）。
   另一個選項是在底部導覽列加第 6 個「更多」按鈕、點開跳出完整選單清單 —— 視覺上比較現代，但要多寫一點程式。
3. **「最新消息」與行事曆的新文案**（#6）：7/3 升八開課已經過期，請給我新的消息與日期，我再改。
4. **線上課程的金流測試碼要不要刪？**（#7）目前使用者點不到，但正式版留著假帳號有點風險。
   刪掉 / 保留 / 搬到獨立檔案，你決定。
5. **高一英文、高二數學、高三四科的聯絡簿**（#4）是「還沒建立部落格」還是「網址漏填」？
   若是漏填，給我網址我直接補上，就不用做成停用外觀了。
6. **Hero 對齊**（#3）：修正後首屏內容會貼底部（原始設計的意圖）。如果你覺得現在貼上面的樣子比較好看，
   那 #3 就不用改 —— 麻煩對照測試頁看一眼再決定。

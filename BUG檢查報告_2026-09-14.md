# 網站 Bug 檢查報告 · 2026-09-14

檢查對象：`index.html`（12,313 行 / 11 段內嵌 JS）、`courses.html`（1,865 行）、
`tracking.html`（464 行）、`dates.json`、`live.json`。

> **檢查方式說明**：這次同樣是排程自動執行。三種實測管道都被擋下 ——
> ① Linux 沙盒因 9/8 的 Windows 更新無法掛載您的資料夾（跑不了 node / 截圖）；
> ② 線上版 `hopehope.net` 的瀏覽器存取權限被拒絕；
> ③ 內建瀏覽器不支援開本機 `file://` 檔案。
> 所以改用完整靜態程式碼分析。下面標「嚴重／中」的項目都有明確的行號與程式碼依據，不是猜測。

**先講結論**：9/12 報告裡的 9 項，**正式版一項都沒有套用**（`index.html` 有更新、長了約 1,300 行，
但補丁沒加上去，所以舊問題全部還在）。這次另外找到 **3 個新問題**，其中第 1 項風險最高。

---

## 一、發現的問題

### 🔴 #1【最高風險・新】家長後台沒有任何驗證，登進去是假資料，而且首頁有按鈕直接連過去

- 位置：`tracking.html` 361–369 行（登入）、383–397 行（假資料）；入口在 `index.html` 8491 行
- 程式碼自己的註解寫著：

```js
// 登入：暫時用本地驗證 (任何 6 碼以上密碼都通過 — 後端接好後改成 API)
if (!email || pwd.length < 6) { showMsg(loginMsg, 'Email 或密碼錯誤', true); return; }
setUser({ email, name: '家長', child: '王小明' });
```

- 也就是說：**任何人**隨便打一個 email ＋ 任意 6 個字，就能進入「家長後台」。
- 進去之後看到的是寫死的假資料：學生「王小明」、本月出席「98%」、
  5 筆打卡（5/13、5/12…）、5 筆段考成績（92 / 88 / 95 / 86 / 84）。
- 而 `index.html`「家長選擇我們的理由 → 成績追蹤」卡片裡有一顆
  **「前往家長後台 →」**按鈕直接連到這一頁，家長點得到。
- 風險：家長會以為那是自己孩子的真實出席與成績；也可能誤以為補習班的系統毫無防護。

### 🔴 #2【嚴重・沿用自 9/12，未修】手機版沒有入口可以進到一半的分頁

- 位置：`index.html` 7788–7789 行（在 `@media (max-width: 720px)` 裡）

```css
.menu-toggle { display: none !important; }
.nav-links  { display: none !important; }
```

- 漢堡選單與整個導覽選單在 720px 以下被 `!important` 關掉，只剩底部導覽列。
- 底部導覽列（12288–12309 行）只有 5 項：**首頁 / 聯絡簿 / 課程 / 線上 / 預約**。
- 而 `.nav-links.active`（6884 行）沒有 `!important`，就算 JS 加上 `active` 也打不開。
- **結果：手機上「關於我們、師資、行事曆、榜單、好評」完全沒有任何入口。**
  （頁尾的連結也救不了，見 #4。）

### 🟠 #3【中・新】手機底部導覽列「目前所在頁」永遠不會亮

- 位置：`index.html` 11831–11844 行 `initMobileBottomNav()`
- 這段程式在第 10950 行那個 `<script>` 裡**立即執行**，
  但 `<nav class="mobile-bottom-nav">` 的 HTML 在第 **12288** 行，比腳本晚才被解析。
- 所以 `document.querySelectorAll('.mob-nav-item')` 抓到 0 個，
  函式在 `if (!items.length) return;` 就結束，MutationObserver 從沒掛上去。
- **結果：五個項目沒有任何一個會變成設計好的黑底白字 `is-active` 狀態**（7779 行的樣式形同虛設）。
  使用者在手機上看不出自己現在在哪一頁。
- 旁證：同一段程式裡的 `initScrollNav`（11637 行）已經有註解寫
  「這段腳本比底部導覽列的 HTML 早執行，所以要延遲查找」並做了處理 ——
  `initMobileBottomNav` 漏做了同一件事。

### 🟠 #4【中・沿用自 9/12，未修】頁尾在每一頁都看不到（桌機也一樣）

- 位置：`index.html` 7043 行 `body.is-paged footer { display: none; }`
- `is-paged` 由 JS 無條件加在 `<body>`（11867 行），所以頁尾永遠是隱藏的。
- 消失的內容：電話、地址、營業時間、LINE / Facebook、關於／聯絡簿快速連結、© 版權。
- 註解寫「一頁一主題模式下會顯得多餘」，**所以有可能是刻意的** —— 這點想請您確認。

### 🟠 #5【中・新】首頁故事區的影片縮圖點下去，彈窗會開但裡面是空的

- 位置：`index.html` 12272–12285 行（`storyVideoLink` 的 click 處理）
- 它只做了 `modal.hidden = false` ＋ 加上 `is-open`，**沒有**呼叫
  `teacherVideoModalsInit()`（10047 行）裡的兩件事：
  - `mountLive(modal)` → 建立「課堂實錄」的 YouTube iframe
  - `window.wangDbLayout()` → 重算「理化 DATABASE」的扇形排版
- **結果：從首頁點 → 空白影片框 ＋ 沒排好的清單；從師資頁點汪老師卡片 → 一切正常。**
  同一個彈窗，兩個入口表現不一樣。

### 🟠 #6【中・沿用自 9/12，未修】首屏（Hero）垂直對齊會跑掉

- `.hero { display: flex !important; align-items: flex-end !important; }`（354–355 行）
- 但 `body.is-paged[data-current="home"] section[data-page="home"] { display: block !important; }`
  （7031–7042 行）特異性更高 → 實際生效的是 `display: block`。
- 結果 `align-items: flex-end` 失效，首屏內容會貼上面而不是貼底部。

### 🟡 #7【小・沿用自 9/12，未修】6 張高中聯絡簿卡片點了會開空白分頁

- 位置：8777 / 8812 / 8837 / 8847 / 8857 / 8867 行
  （高一英文、高二數學、高三物理 / 化學 / 數學 / 英文）
- `href="javascript:void(0)"` 卻同時帶 `target="_blank"` → Chrome 點下去會開一個 `about:blank` 空白頁。
- 而且 `title="最新聯絡簿"`、`data-status="active"`，外觀跟可用的卡片一模一樣。
- （日期文字本身沒問題，JS 會正確顯示「尚未上線」。）

### 🟡 #8【小・新】`courses.html` 有「國六」這個不存在的年級，章節也是示範資料

- 位置：`courses.html` 1360 行

```js
g6:  { name: '國六', tag: 'GRADE 6', num: '6', chapters: ['第1章 數的世界', …] },
```

- 「國六」不是台灣的學制用詞（應為「小六」，或根本不該出現 —— 網站主打國七～高三）。
- 而且 1358 行的註解寫「章節資料（示範用，可以替換）」：
  國七列的是理化章節、高一高二列的是化學章節，都不是實際課程內容。
- 七張卡片一律顯示「**已開放**」，但 `index.html` 的線上課程區把高一高二標成「敬請期待」，兩邊說法不一致。
- 這些只有登入後（或 `?preview=empty`）才看得到，**我沒有動它**，需要您給正確內容。

### 🟡 #9【小・沿用】首頁「最新消息」已經過期兩個多月

- 彈窗（8229 行）與行事曆卡片（10184 行）都還是
  「**7 月 3 日 升八年級理化開課 · 現在開始登記，預約劃位中**」。
- 今天是 9/14，已經過了兩個多月。行事曆第二張卡也還是「TBA 敬請期待」。
- 內容問題，**我沒有動它**，需要您給新文案。

### 🟡 #10【小・沿用】正式版裡留著「測試模式」的金流程式碼

- 9187 行假匯款帳號 `0000-000-000000`
- 9196 行「測試模式：點下方按鈕會立刻開通章節」
- 9524 行佔位 LINE 群 `https://line.me/R/ti/p/@example`
- 9541 行 `payDoneBtn` 按下去直接寫 localStorage 開通，不經過任何付款
- 目前 `#onlinePanelG12` 是 `hidden style="display:none"`，六個年級卡片都直接連到 `courses.html`，
  **所以使用者點不到**。但這段程式碼還在正式版裡，哪天把面板打開就會外露。

### 🟡 #11【小】自動更新的資料檔停在 9/12

- `dates.json` 的 `updatedAt` 是 `2026-09-12T17:32`（GitHub Actions 原本每小時跑）
- `live.json` 的 `checkedAt` 是 `2026-09-12T00:00`（原本每三小時跑）
- 今天是 9/14，兩個都停了約兩天。
- **但這可能只是您本機沒有 `git pull`** —— 線上版不一定一樣。
  若線上版也停在 9/12，就要去看 GitHub Actions 是不是壞了。

### 🟡 #12【小】其他零碎

- **HTML 無效屬性**：10813 行 Google 地圖 `<iframe … loading="lazy" decoding="async">`，
  `decoding` 不是 iframe 的屬性（只有 `<img>` 有）。不影響顯示，但驗證器會報錯。
- **`courses.html` 標籤巢狀不合法**：1553–1554 行把 `<button class="grade-add-btn">`
  放在 `<a class="online-grade-card">` 裡面。瀏覽器能跑（程式有做 `stopPropagation`），但不合 HTML 規範。
- **死碼**：7060–7082 行整組 `body.mobile-pager` 規則（JS 實際用的是 `is-paged`）；
  6888 行 `.logo-sub` 選擇器在 HTML 裡不存在；12255 行 `initBloggerHomework()` 是空函式。
- **錯字**：HTML 註解裡「手機**板**」應為「手機**版**」（2635、8915 行，不會顯示給使用者）；
  資料夾裡 `繳學金申請pdf.pdf` 應為「**獎**學金」（此檔沒被任何頁面引用）。
- **沒被引用的檔案**：`115下學期七年級.pdf`、`115下學期八年級.pdf` 兩個新 PDF 目前沒有任何頁面連到。

---

## 二、檢查通過、沒有問題的部分

| 檢查項 | 結果 |
|---|---|
| 首頁結構 | ✅ 17 個 `<section>` 的 `data-page` 全部對應到 10 個分頁，無孤兒區塊 |
| 導覽列連結 | ✅ 10 個 `#` 錨點（home/about/courses/dm/online/teachers/calendar/honor/testimonials/contact）對應的 `id` 全部存在 |
| 重複 id | ✅ 85 個 `id` 全部唯一，無重複 |
| 圖片破圖 | ✅ 66 個本機圖片引用（含 `.JPEG` 教室照、`scholarship/page-1~9.jpg`）檔案都在，無破圖 |
| 外部連結 | ✅ 40 個 `target="_blank"` **全部**都有配 `rel="noopener"`；15 個 blogspot、6 支 YouTube、LINE、FB、Google Maps 格式都正確 |
| 內頁連結 | ✅ `courses.html`、`tracking.html` 存在，回連 `index.html` 也正常 |
| **`steneneng-7` 疑似錯字 → 已確認不是錯字** | ✅ `dates.json` 顯示機器人在 2026-09-08 成功從 `steneneng-7.blogspot.com` 抓到文章。這個帳號是真的存在的，**之前三份報告問的這一題可以結案了** |
| 聯絡簿日期 | ✅ 15 個年級科目，`dates.json` 有 15 筆資料；前端有 localStorage 快取 ＋ 抓失敗的備援文字，不會卡在「載入中...」 |
| JS 語法 | ✅ `index.html` 11 段、`courses.html` 1 段、`tracking.html` 1 段內嵌腳本，逐段人工比對括號與結構，沒有找到語法錯誤（9/6 那個多餘 `});` 的問題已經不在了） |
| 按鈕 / 彈窗邏輯 | ✅ 最新消息、老師試聽影片 ×4、DM 燈箱、榜單燈箱、獎學金放大、教室照片、FAQ、國中／高中切換、Hero 輪播、小飛吉祥物 —— 開關與 Esc 關閉邏輯都完整（`storyVideoLink` 例外，見 #5） |
| 榜單搬移邏輯 | ✅ `relocateHonor()`（10589 行）把榜單搬到 `#honor-mount`，位置在 body 直屬層，不會被別的分頁包住而消失 |
| 文案年數 | ✅ 已統一為「30 年 / 三十年 / 近 30 年」；8448 行的「二十年」是大事紀裡講 20 週年，屬正確用法 |
| 諮詢表單 | ✅ 有真的送出去（隱藏 iframe POST 到 Google 表單），不是只跳成功訊息；送出中會 disable 按鈕 |
| 手機縮放安全設定 | ✅ 7815 行起有針對手機縮放破圖的處理（關掉 mix-blend-mode、降低 blur 半徑） |

---

## 三、已修正的內容（都在測試檔，正式版一行都沒動）

| 檔案 | 內容 |
|---|---|
| `bugfix-patch.css`<br>（沿用 9/12，未改） | #2 手機版把漢堡選單放回來、`.nav-links.active` 補 `!important` 讓它打得開<br>#4 只在「預約諮詢」分頁把頁尾放回來（保守做法）<br>#6 用更高特異性把 Hero 的 `display:flex` + `align-items:flex-end` 拿回來<br>#7 停用中的聯絡簿卡片改成明顯的灰階／不可點外觀 |
| `bugfix-patch.js`<br>（**本次新增 2 項**） | #7 把 `javascript:void(0)` 的卡片標成 `aria-disabled`、移除 `target`、擋掉點擊（沿用）<br>#12 移除 iframe 上無效的 `decoding` 屬性（沿用）<br>從底部導覽切分頁時順手收起手機選單（沿用）<br>**#3【新】在 DOMContentLoaded 後重新實作 `initMobileBottomNav`，讓底部導覽列的 `is-active` 正常運作**<br>**#5【新】讓 `storyVideoLink` 轉交給 `#teacherWang` 的完整流程，影片與扇形排版才會出現** |
| `bugfix-patch-tracking.js`<br>（**本次新增檔案**） | #1 在 `tracking.html` 最上方加「示範版面 · 尚未與補習班系統連線」橫幅、<br>登入卡片下方加說明、出席／成績面板加「示範資料」標籤。<br>**只做誠實標示，沒有改任何登入邏輯** —— 真正要怎麼處理請看第五節 |
| `bugfix-test.html`<br>（更新說明文字） | 對照測試頁：把 `index.html` 嵌進 iframe 再即時注入補丁，有「修正開／關」切換和桌機／平板 820／手機 390 三種寬度 |
| `bugfix-test-tracking.html`<br>（**本次新增檔案**） | 同上，但測的是 `tracking.html`，多一顆「清掉登入狀態」按鈕方便重測 |

### 怎麼看效果

1. 先跑 `啟動本機伺服器.bat`（一定要用 http，直接雙擊開檔會被瀏覽器 `file://` 限制擋住，補丁注不進去）。
2. 瀏覽器開 `http://localhost:<埠號>/bugfix-test.html`。
3. 選「手機 390px」，按右上角「修正：已套用 / 關閉（原始版）」來回切：
   - #2：看得到／看不到漢堡選單
   - #3：底部導覽列有沒有一項變成黑底白字
   - #5：首頁故事區那張影片縮圖點下去，彈窗裡有沒有影片
4. 再開 `http://localhost:<埠號>/bugfix-test-tracking.html` 看 #1 的標示差別。

### 確認 OK 後要怎麼正式套用

`index.html` 加兩行就好（不用改任何現有程式碼）：

```html
<!-- </head> 前 -->
<link rel="stylesheet" href="bugfix-patch.css" />
<!-- </body> 前 -->
<script src="bugfix-patch.js"></script>
```

`tracking.html` 加一行（但請先看第五節第 1 題再決定）：

```html
<!-- </body> 前 -->
<script src="bugfix-patch-tracking.js"></script>
```

---

## 四、修改到的檔案

**本次新增 3 個檔：**

- `bugfix-patch-tracking.js`
- `bugfix-test-tracking.html`
- `BUG檢查報告_2026-09-14.md`（本檔）

**本次修改 2 個檔：**

- `bugfix-patch.js`（加入 #3、#5 兩項修正）
- `bugfix-test.html`（只更新日期與說明文字）

**完全沒有動到：** `index.html`、`courses.html`、`tracking.html`、`dates.json`、`live.json`、
`bugfix-patch.css`、所有圖片。

> 另外提醒（第三次了）：資料夾裡的 `index_bugfix_test.html` 是 9/5 版 `index.html` 的整份副本，
> 已經比現在的正式版舊了 1,200 多行。我沒有動它，但建議刪掉以免哪天拿錯檔。

---

## 五、需要您確認的地方

1. **家長後台（#1）要怎麼處理？** 這是這次最該優先決定的一題。三個選項：
   - **(a) 先下架**：把 `index.html` 8491 行那顆「前往家長後台」按鈕拿掉，
     等後端接好再放回來 —— 最乾淨，我建議這個。
   - **(b) 保留但誠實標示**：套用 `bugfix-patch-tracking.js`，明講是示範版面。
   - **(c) 接真正的後端驗證**：要另外規劃，不是改幾行能解決的。

2. **頁尾要不要顯示？（#4）** 程式碼註解說是刻意隱藏的。我目前只在「預約諮詢」頁放回來。
   想每頁都顯示的話，把 `bugfix-patch.css` 裡的選擇器改成 `body.is-paged footer`；
   想維持全部隱藏，就把那一段刪掉。

3. **手機導覽想用哪種解法？（#2）** 我選的是「把漢堡選單放回來」（改動最小）。
   另一個選項是在底部導覽列加第 6 個「更多」按鈕、點開跳出完整選單清單 ——
   視覺上比較現代，但要多寫一點程式。

4. **「最新消息」與行事曆的新文案（#9）**：7/3 升八開課已經過期兩個多月，請給我新的消息與日期。

5. **`courses.html` 的年級與章節（#8）**：
   - 「國六」要改成「小六」、還是整個拿掉？
   - 各年級的章節清單要換成真的內容嗎？有的話給我一份，我來替換。
   - 高一高二在 `courses.html` 寫「已開放」、在 `index.html` 寫「敬請期待」，要以哪個為準？

6. **線上課程的金流測試碼要不要刪？（#10）** 目前使用者點不到，但正式版留著假帳號有風險。
   刪掉 / 保留 / 搬到獨立檔案，您決定。

7. **`dates.json` / `live.json` 停在 9/12（#11）**：
   麻煩看一下 GitHub 上的檔案是不是也停在 9/12。如果是，我再去看 Actions 的問題；
   如果線上是新的，那就只是本機沒 pull，不用管。

8. **要不要恢復真實瀏覽器實測？** 這次三個管道都被擋。最快的方式是：
   在對話中開一次 `hopehope.net`（或 GitHub Pages 網址）並核准存取，
   之後排程就能直接跑線上版的點擊測試與截圖，而不是只看程式碼。
